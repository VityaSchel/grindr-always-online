[package]
name = "grindr-always-online"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
grindr = "0.2"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
serde_json = "1"
rand = "0.10"
